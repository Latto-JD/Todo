# 로컬 (replica set 없는 standalone도 동작 — 트랜잭션은 순차 쓰기로 폴백)
MONGODB_URI=mongodb://localhost:27017/omc-todo

# MongoDB Atlas / Vercel 배포 (replica set → 트랜잭션 사용)
# MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/todoapp?retryWrites=true&w=majority
